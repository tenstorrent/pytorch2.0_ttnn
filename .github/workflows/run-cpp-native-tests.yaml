name: "C++ Extension Tests"

on:
  push:
    branches:
      - main
    paths:
      - 'torch_ttnn/cpp_extension/**'
      - 'tests/cpp_extension/**'
      - 'pyproject.toml'  # Trigger on ttnn dependency updates
  pull_request:
    paths:
      - 'torch_ttnn/cpp_extension/**'
      - 'tests/cpp_extension/**'
      - 'pyproject.toml'  # Trigger on ttnn dependency updates
  workflow_dispatch:
    inputs:
      docker_tag:
        description: "Docker container tag to use"
        required: false
        type: string
        # Using tt-metal Docker image instead of building pytorch2.0_ttnn images
        # Rationale: tt-metal images are public and provide all needed dependencies
        # pytorch2.0_ttnn images require authentication and are built on top of tt-metal
        # PINNED to specific digest for reproducibility (Nov 18, 2025)
        # Digest from: docker manifest inspect --verbose ghcr.io/tenstorrent/tt-metal/tt-metalium/ubuntu-22.04-ci-test-amd64:latest
        # To update: Get new digest and update the sha256 hash below
        default: "ghcr.io/tenstorrent/tt-metal/tt-metalium/ubuntu-22.04-ci-test-amd64@sha256:104b062b1942b398902884d6340a9695fbfcd4ffe6fb5a59f3d6363c499e7fce"

jobs:
  cpp-extension-tests:
    runs-on: ["in-service", "nfs"]
    container:
      # Using tt-metal public Docker image (not building pytorch2.0_ttnn images)
      # Pinned to specific digest for reproducibility and project freeze (Nov 18, 2025)
      # Rationale: Ensures consistent build environment across all CI runs and prevents
      # unexpected breakages from upstream image updates. Manual override available via workflow_dispatch.
      image: ${{ github.event.inputs.docker_tag || 'ghcr.io/tenstorrent/tt-metal/tt-metalium/ubuntu-22.04-ci-test-amd64@sha256:104b062b1942b398902884d6340a9695fbfcd4ffe6fb5a59f3d6363c499e7fce' }}
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
      options: >-
        --rm -v /dev/hugepages-1G:/dev/hugepages-1G --device /dev/tenstorrent
        -v ${{ github.workspace }}:${{ github.workspace }} -w ${{ github.workspace }}
        -v /mnt/tt-metal-pytorch-cache/.cache:/root/.cache
    env:
      CCACHE_DIR: /root/.cache/ccache
      CPM_SOURCE_CACHE: /root/.cache/cpm
      CCACHE_BASEDIR: ${{ github.workspace }}
      CCACHE_MAXSIZE: 30G
      PYTHON_ENV_DIR: ${{ github.workspace }}/torch_ttnn/cpp_extension/third-party/tt-metal/python_env
    defaults:
      run:
        shell: bash
    steps:
      - name: Prepare checkout
        run: |
          set -euxo pipefail
          apt-get update
          apt-get install -y git-lfs
          git lfs install

      - uses: actions/checkout@v4
        with:
          lfs: true
          fetch-depth: 0
          submodules: recursive  # Let GitHub Actions handle all submodules
          clean: true            # Clean working tree before checkout

      - name: Configure git safe directories
        run: |
          set -euxo pipefail
          git config --global --add safe.directory "${GITHUB_WORKSPACE}"
          git config --global --add safe.directory '*'
      
      - name: Initialize git submodules
        run: |
          set -euxo pipefail
          git submodule update --init --recursive
      
      - name: Install system dependencies
        run: |
          set -euxo pipefail
          apt-get update
          apt-get install -y build-essential clang-17 clang-format-17 llvm-17-dev gcc-12 g++-12 cmake ninja-build \
            python3 python3-venv python3-pip curl jq ccache nlohmann-json3-dev

      - name: Configure ccache
        run: |
          set -euxo pipefail
          mkdir -p "${CCACHE_DIR}"
          ccache --set-config=cache_dir="${CCACHE_DIR}"
          if [[ -n "${CCACHE_MAXSIZE:-}" ]]; then
            ccache --set-config=max_size="${CCACHE_MAXSIZE}"
          fi
          if [[ -n "${CCACHE_BASEDIR:-}" ]]; then
            ccache --set-config=base_dir="${CCACHE_BASEDIR}"
          fi
          ccache --set-config=compression=true
          ccache --zero-stats

      - name: Install TT-Metal system dependencies
        run: |
          set -euxo pipefail
          cd torch_ttnn/cpp_extension/third-party/tt-metal
          ./install_dependencies.sh
      
      - name: Build C++ extension
        run: |
          set -euxo pipefail
          
          # Ensure git safe.directory is set for git commands
          git config --global --add safe.directory "${GITHUB_WORKSPACE}" || true
          
          # Check if this is a ttnn update PR (for testing new ttnn version from PyPI)
          if git log --oneline -1 | grep -q "Update dependencies to"; then
            # Test new ttnn version from PyPI
            bash torch_ttnn/cpp_extension/build_cpp_extension.sh Release --test-pypi-ttnn
          else
            # Regular build with local ttnn
            bash torch_ttnn/cpp_extension/build_cpp_extension.sh Release
          fi
      
      - name: Run tests
        run: |
          set -euxo pipefail
          
          # Run tests using the test runner script
          bash torch_ttnn/cpp_extension/run_cpp_extension_tests.sh
          
          # Show ccache statistics
          ccache --show-stats

  tests-passed:
    if: ${{ always() }}
    outputs:
      didpass: ${{ steps.check.outputs.didpass }}
    runs-on: ubuntu-latest
    needs: [cpp-extension-tests]
    steps:
      - id: check
        run: |
          cpp_tests_result="${{ needs.cpp-extension-tests.result }}"
          if [[ $cpp_tests_result == "success" || $cpp_tests_result == "skipped" ]]; then
            echo "didpass=0" >> $GITHUB_OUTPUT
            exit 0
          else
            echo "didpass=1" >> $GITHUB_OUTPUT
            exit 1
          fi
